function calcularRaizQuadrada(numero) {
    return Math.sqrt(numero);
  }
  
  const numeroExemplo = 25;
  const raizQuadrada = calcularRaizQuadrada(numeroExemplo);
  console.log("Raiz quadrada:", raizQuadrada);
  