function calcularAreaTriangulo(base, altura) {
    return (base * altura) / 2;
  }
  
  const baseTriangulo = 5;
  const alturaTriangulo = 8;
  console.log("Área do triângulo: " + calcularAreaTriangulo(baseTriangulo, alturaTriangulo));
  